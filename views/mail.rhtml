<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
	<title><%=h @config[:cgi_title]%>(<%=h folder%>) / <%=h subject%></title>
    <link rel='stylesheet' type='text/css' href='css/style.css'>
  </head>

  <body>

    <div class='header'>
      <% @config[:folders].each do |f| %>
        <% query = {:f => u(f)} %>
        <% if f == folder %>
          <span class='folder_sel'><a href='<%=cgi_link(query)%>'>[<%=h f%>]</a></span>
        <% else %>
          <span class='folder'><a href='<%=cgi_link(query)%>'><%=h f%></a></span>
        <% end %>
      <% end %>
    </div>

    <hr>

    <div class='mail'>
      <% if mail.nil? %>
        mail <%=h mailnum%> was not found.
      <% else %>
        <pre><%=h header%></pre>
        <hr>
        <pre><%=h body%></pre>
      <% end %>
    </div>

    <hr>

    <div class='footer'>
      Generated by mail2www<br>
      Powered by <a href='http://www.ruby-lang.org/'>Ruby</a> version <%=h RUBY_VERSION%>
    </div>

  </body>
</html>
