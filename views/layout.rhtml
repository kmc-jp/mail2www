<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title><%=h @title %></title>
    <link rel='stylesheet' type='text/css' href='<%=@config[:prefix]%>/css/style.css'>
    </head>

  <body>
    <div class='header'>
      <% @config[:folders].each do |f| %>
        <% if f == folder %>
          <span class='folder_sel'><a href='<%=@config[:prefix]%>/<%= f %>'>[<%=h f%>]</a></span>
        <% else %>
          <span class='folder'><a href='<%=@config[:prefix]%>/<%= f %>'><%=h f%></a></span>
        <% end %>
      <% end %>
    </div>
    <hr>

    <%= yield %>

    <div class='footer'>
      Generated by mail2www<br>
      Powered by <a href='http://www.ruby-lang.org/'>Ruby</a> <%=h RUBY_VERSION%>
    </div>
  </body>
</html>
